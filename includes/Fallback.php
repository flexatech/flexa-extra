<?php
/**
 * Loaded only when a second copy of the plugin is already active, to allow the
 * duplicate to deactivate itself cleanly without fatal errors.
 *
 * @package flexatech/flexa-extra
 */

defined( 'ABSPATH' ) || exit;
